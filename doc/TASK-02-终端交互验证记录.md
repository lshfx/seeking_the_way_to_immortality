# TASK-02 终端交互验证记录

- 更新日期：2026-09-22
- 状态：**实现与自动化检查已完成；目标终端矩阵尚未全部实测，TASK-02 保持进行中。**
- 范围：验证交互、布局和终端恢复，不实现游戏规则、时间、随机数或存档。
- 正式语言仍未选定。当前探针用 Python 3.13.5 和标准库快速验证终端行为，不据此决定正式技术栈。

## 可运行试验

代码位于 `tools/task02-terminal-probe/`，不依赖原型目录。原来的 `prototypes/` 按项目忽略规则继续保留为参考资料。

```powershell
cd tools/task02-terminal-probe
python interactive_probe.py
python -m unittest -v test_interactive_probe.py
```

键盘操作：`W/S` 或方向键移动，`1`～`3` 选择，`Enter` 只显示诊断预览，`n/p` 或翻页键切换场景页，`Tab` 在选项和说明间切换焦点，`Esc` 返回选项，`q` 退出。窗口尺寸变化时每 100 毫秒重新取终端尺寸并重画；尺寸不够时只显示扩大窗口提示。

交互预览不会执行游戏行动：探针没有世界时钟、随机源、引擎或存档，也不访问网络和工作目录数据。文本中的 ESC、响铃等控制字符会被过滤；CSI/OSC 输入序列会被消费，不会当作菜单按键；括号粘贴模式退出时会复位。退出路径恢复光标、颜色/粘贴模式以及控制台输入回显设置。

## PanelModel 草案

`interactive_probe.py` 中不可变的 `PanelModel` 仅承载展示所需的数据：

```text
PanelModel:
  title, status, scene, story_pages[], risk, choices[], details[]

ProbeState（仅为界面试验状态）:
  page, selected, focus, message
```

查询和重排只读取 `PanelModel`；翻页、缩窗、焦点切换和预览只改 `ProbeState`。此草案不是 TASK-04 的 `GameState` 或最终接口。

布局按 Unicode East Asian Width 计算全宽字符与组合字符，框线使用 ASCII；超长单行以 `...` 明示截断。布局快照固定覆盖 80×24、64×20、48×16，目标尺寸内选项和退出键都必须可见。小于 48×16 时仅提示扩大窗口，不静默截掉选择。

## 自动化结果

运行 `python -m unittest -v test_interactive_probe.py`：**9 项通过**。检查内容包括：

- 80×24、64×20、48×16 每行宽度精确，三项选择与退出键可见；40×10 显示尺寸提示。
- 超长姓名不会推走选项或退出提示；组合字符裁切和宽度计算保持完整。
- 真彩使用 24 位色码，基础色只用基础 ANSI 色码，无色档不输出颜色控制码。
- 方向选择、页切换、焦点和尺寸重排不触发游戏结算。
- CSI 方向键序列能解析；OSC 内容和括号粘贴中的 `q`、数字不会变成游戏按键。
- EOF 与 Ctrl+C 输入映射到安全退出；实际终端中 Ctrl+C/EOF 的模式恢复仍待人工操作记录。

此外，使用 Codex 提供的 Windows PowerShell PTY 以 80×24 实际启动一次：按 `2` 移动选择、回车预览、按 `n` 翻页、按 `q` 退出；预览明确显示“不扣月、不修改角色、不存档”，页码变化且所选项保留。退出后在同一 PowerShell PTY 执行 `Read-Host`，输入 `echo-restored` 成功回显并读取，证明这次正常退出恢复了输入回显。该记录只覆盖当前 PTY，不能代表其他终端产品。

## 当前终端支持矩阵

| 环境/检查 | 当前证据 | 状态 |
|---|---|---|
| Codex Windows PowerShell PTY，80×24，无色档 | 实际键盘选择、预览、翻页、`q` 退出及后续 `Read-Host` 回显 | 已验证 |
| 80×24、64×20、48×16 静态布局 | 自动化快照断言行宽、选择项和退出键 | 已验证布局；尚未逐个真机缩放 |
| 低于 48×16 | 40×10 自动化快照提示，不显示菜单 | 已验证布局 |
| 真彩/基础色/无色 | ANSI 输出自动化断言；基础色无 `38;2`，无色无颜色码 | 已验证输出；真实视觉效果待测 |
| Windows Terminal | 尚无独立人工记录 | 待测 |
| VS Code 集成终端 | 尚无独立人工记录 | 待测 |
| 传统 conhost / cmd | 尚无独立人工记录 | 待测 |
| 实际拖动缩放、Ctrl+C、EOF 中断恢复 | 代码路径已实现，尚无独立终端操作记录 | 待测 |
| 字体 | 当前 PTY 能显示中文；具体字体未知，Sarasa Mono / Cascadia Mono 未逐一测试 | 待测 |

因此本报告不宣称已经支持所有 Windows 终端或字体。下次在目标终端运行探针并补上尺寸、色彩档、退出方式和实际结果后，才能关闭 TASK-02。`Ctrl+C` 的清理路径由 `finally` 执行；如中断信号无法到达程序，记录为终端/PTY限制，不得标成通过。
